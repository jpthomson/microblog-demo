<div class="row">
    <div class="six columns">
        
        <div class="radius panel">
            <h5 class="title"><%= @tag %></h5>
        </div>

        <%= render @bleats %>

    </div>
    <div class="six columns">
        <% unless signed_in? %>
            <div class="radius callout panel">
                <h5>Welcome to Bleater!</h5>
                <p><a href="#">Sign in</a> or <a href="#">sign up</a> to get started.</p>
            </div>
        <% end %>
    </div>
</div>
